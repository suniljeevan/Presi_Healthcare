server:
 port: 9296
 
spring:
  application:
   name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/suniljeevan/Presi_Healthcare
          clone-on-start: true
     
